    //Snake - using Queue;
    //Head of snake - last element of queue, so can add new element there
    //Tail of snake - first element of queue, so can dequeue it.
    //Moving - Dequeue (remove tail of snake) and Enqueue new position of the head of snake.